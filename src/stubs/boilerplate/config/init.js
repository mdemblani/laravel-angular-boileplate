import angular from 'angular';

/**
 * @fileOverview
 * This is the main function from which kickstarts the entire angular application. This function is
 * defined as the run block for the main angular module. Keep this block as thin as possible.
 *
 * Any application level initalization required should be done here, such as
 * 1. Requesting logged-in user information
 * 2. Requesting server for application level settings and variables
 * 3. Setting up the basic application level globals
 */

function init() {
	'ngInject';

	// Add your application kickstart code here.
}

export default init;